
data {
for (i in 1:100)
  fem_P[i] ~ dpois(rate[i])
  
  rate[i] <- rate[i]
  }
}
model {
 for (i in 1:100) {
  p_Age[i] ~ dgamma(shape[i], rate[i])
  
  shape[i] <- pow(m,2) / pow(sd,2)
  rate[i] <-     m    / pow(sd,2)
  }
    m ~ dunif(0,100)
    sd ~ dunif(0,100)
}

